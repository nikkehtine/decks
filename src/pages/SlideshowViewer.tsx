import { Viewer } from "@/components/slideshow/Viewer";

export const SlideshowViewer = () => (
  <main className="fixed inset-0 grid place-items-center bg-white dark:bg-black">
    <Viewer />
  </main>
);
