import { Viewer } from "@/components/slideshow/Viewer";

export const SlideshowViewer = () => (
  <main className="grid min-h-dvh place-items-center bg-white dark:bg-black">
    <Viewer />
  </main>
);
